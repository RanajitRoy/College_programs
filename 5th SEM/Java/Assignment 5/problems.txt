Create a class called time that has separate integer member data for hour, minute & second. one constructor should initialize dates to 0 and another should initialize it to fixed values. A main program should create 2 initialized time object and one that is not initailized. then it should add 2 initailized values to store in a third time type variable. finally it should display the value of the third variable. Take input using Scanner class.
